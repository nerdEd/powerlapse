!!! 5
%html(lang="en")
  %head
    %meta(charset="utf-8")
    %meta(http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title= content_for?(:title) ? yield(:title) : "Powerlapse"
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      = javascript_include_tag "http://html5shim.googlecode.com/svn/trunk/html5.js"
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag :application
    %link{href: asset_path("apple-touch-icon-144x144.png"), rel: "apple-touch-icon-precomposed", sizes: "144x144"}
    %link{href: asset_path("apple-touch-icon-114x114.png"), rel: "apple-touch-icon-precomposed", sizes: "114x114"}
    %link{href: asset_path("apple-touch-icon-72x72.png"), rel: "apple-touch-icon-precomposed", sizes: "72x72"}
    %link{href: asset_path("apple-touch-icon.png"), rel: "apple-touch-icon-precomposed"}
    %link{href: asset_path('favicon.ico'), rel: "shortcut icon"}

    :javascript
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-378745-11']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();


  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.brand(href="#{root_url}")
            = image_tag 'logo.png', height: 30, width: 186
          %a.brand.pull-right{href: "http://www.inthebackforty.com", style: "float: right !important;"}
            by
            = image_tag 'http://www.inthebackforty.com/assets/logo.png', height: height = 30, width: height * 225 / 44

    .container

      .row
        .span9
          = yield
        .span3
          .well.sidebar-nav
            %ul.nav.nav-list
              %li.nav-header Key
              %li #{image_tag('MapDot_Pink.jpeg')} 1-15
              %li #{image_tag('MapDot_Purple.jpeg')} 16-30
              %li #{image_tag('MapDot_Blue.jpeg')} 31-50
              %li #{image_tag('MapDot_Green.jpeg')} 51-250
              %li #{image_tag('MapDot_Yellow.jpeg')} 251-1,000
              %li #{image_tag('MapDot_Orange.jpeg')} 1,001-2,500
              %li #{image_tag('MapDot_Red.jpeg')} 2,500-10,000

            %h4 What am I looking at?
            %p This is a collection of BGE power outage maps over the course of hurricane Sandy. The key above indicates the size of each outage plotted on the map.

            %h4 How does it work?
            %p With the slider in the rightmost position you're looking at the most recent BGE outage map. Drag the slider to the left to see outage maps from earlier.

            %h4 Are you affiliated with BGE?
            %p Nope.

            %h4 Who are you?
            %ul
              %li= link_to 'Ed Schmalzle', 'http://www.edschmalzle.com'
              %li= link_to '@nerded', 'http://twitter.com/nerded'
              %li= link_to 'Back Forty', 'http://www.inthebackforty.com'

            %h4 Sources
            %p We're using the public BGE outage map page to populate this timelapse. You can see it on their website #{link_to 'here', 'http://www.bge.com/customerservice/stormsoutages/currentoutages/pages/default.aspx'}.
    
      %footer
        %p &copy; Ed Schmalzle 2012
    /
      Javascripts
      \==================================================
    / Placed at the end of the document so the pages load faster
    = javascript_include_tag "application"
